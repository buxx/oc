[package]
name = "oc_root"
version = "0.1.0"
edition = "2024"

[features]
bevy = ["dep:bevy"]
debug = []

[dependencies]
rkyv = { workspace = true, features = ["uuid-1"] }
derive_more = { workspace = true, features = ["constructor", "deref", "from", "into", "add", "mul"] }
serde = { workspace = true, features = ["serde_derive"] }
serde_derive = { workspace = true }
glam.workspace = true
bevy = { workspace = true, optional = true }
uuid.workspace = true
